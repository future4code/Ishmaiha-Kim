export type userArray = {
    id: string,
    name: string,
    email: string,
    age: number
}

export type productArray = {
    id: string,
    name: string, 
    description: string,
    price: number
}